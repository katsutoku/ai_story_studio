# AI Story Studio - プロジェクト方針（Claude Code向け）

このファイルは、Claude.ai（Web版）での長い設計相談の要点をまとめたものです。
Claude Codeでこのリポジトリを触る際は、必ずこの内容を踏まえて作業してください。

## プロジェクトの目的

ADV・ノベルゲームのポータルサイトを作りたいが、コンテンツ（シナリオ）が不足している、
という悩みが発端。**用途はゲームのシナリオ生成に特化**しており、汎用の小説執筆ツールでは
ない。利用者は「漫画の編集担当」のように、AIに執筆を任せつつ内容を確認・調整する立場。

## 技術スタック

- Flask + SQLAlchemy + SQLite（`instance/ai_story_studio.db`）
- Flask-WTF（フォーム・CSRF保護。**全POSTフォームにcsrf_tokenが必須**）
- Bootstrap5 + Jinja2（サーバーサイドレンダリング、SPAではない）
- AI連携：OpenAI / Gemini / Claude / Ollama をプロバイダ切り替え可能（`app/ai_service.py`）
- 図表描画：vis-network（人物相関図。Mermaidから移行済み、理由は後述）

## 開発方針（最重要）

**複雑さを避け、小さく積み上げる。** これがプロジェクト全体を通しての一貫した方針。

- 大きな設計変更（シーン/フラグ/選択肢分岐/マルチエンディングのような「ゲームエンジン」的機能）は
  意図的に見送っている（詳細は「見送った・保留にした機能」を参照）。安易に手を広げない。
- 新機能は、既存の設計パターン（後述の「AI生成→プレビュー→選択保存」など）を横展開する形で
  実装し、新しい仕組みを増やしすぎない。
- 動作確認は口約束にせず、実際にFlaskのテストクライアント等で検証してから完了とすること。

## アーキテクチャ上の重要な設計パターン

### 1. AI生成は「生成 → プレビュー → 選択保存」の3段階

キャラクター・世界設定・伏線・全体プロットのAI生成は、**生成結果を即DB保存しない**。
プレビュー画面でチェックボックスにより選択させてから確定保存する（`characters.generate` /
`generate_confirm` 等のペア構成）。理由：AIの出力を無検証で確定させると誤りに気づけないため。
新しくAI生成機能を追加する場合もこのパターンを踏襲すること。

### 2. AIプロバイダの抽象化（`app/ai_service.py`）

`_dispatch(prompt, provider)` が openai/gemini/claude/ollama を振り分ける共通口。
新しいAI生成機能を作るときは、既存の `_generate_with_*` 関数をそのまま再利用できる。
プロンプト構築（`build_*_prompt`）とAPI呼び出しは分離されている。

### 3. 章本文の書式規約（`build_prompt()` に常時注入）

- セリフ行は必ず `【発言者名】「セリフ」` の形式（ゲーム用スクリプト変換のため）。
  **発言者名は常に実名を使う**（正体を隠す「？？？」表記は検討したが、
  それはゲームのシナリオシステム側でフラグ管理するか人が対応する領域と判断し、
  明示的に却下・実装しなかった。復活させないこと）。
- 本文の先頭、および章内で場面（場所・時間帯）が転換するたびに、
  `■背景:（短い地名。画像アセット判別用、時間帯等の修飾語を含めない）` と
  `■場面:（時間帯等を含む場面名、表示用）` の2行を挿入する。
- 各章は「起承転結が完結する小さなエピソード」として書き、同時に最終章への手がかりを
  さりげなく仕込む（逆転裁判のような話数完結型の構成を志向）。
- `Project.constraints`（禁止事項・既知の事実欄）は、章生成・全体プロット生成・
  キャラクター/世界設定/伏線生成の**すべてのプロンプトに常時注入**される。
  ミステリーのアリバイ矛盾防止や「主人公が犯人と悟らせない」等の制御に使う。

### 4. DBマイグレーションは手動（Alembic等は未導入）

`db.create_all()` は新規テーブルしか作らず、既存テーブルへの列追加はしない。
モデルにカラムを追加したら、**必ず `migrate_db.py` の `COLUMNS_TO_ENSURE` リストにも追記**し、
利用者に最新版を渡すこと（これまで何度も発生した忘れがちな作業）。

### 5. ファイル資産はサーバーのローカルディスクに保存（DBにはファイル名のみ）

章本文（`data/chapters/`）、キャラクターサムネイル（`data/character_thumbnails/`）は
この方式。**この設計は「アプリを1台のサーバーに集約してホスティングする」運用を前提**にしており、
チームの各メンバーがそれぞれローカルでアプリを起動する運用だと、
ファイルを登録した人のPC上にしかない、という問題が起きる（利用者にも明示済みの既知の制約）。
将来チーム共有を強化する場合はクラウドオブジェクトストレージへの移行を検討する。

### 6. 改行コードの正規化

ブラウザのtextarea送信は `\n` を `\r\n` に変換するため、
本文の保存・読み込みの全箇所で `_normalize_newlines()` を必ず通す
（`app/routes/chapters.py`）。新しくtextarea経由のテキスト保存処理を作る場合も同様の対応が必要。

### 7. 人物相関図のキャッシュと更新検知

`Project.relationship_diagram_json` にキャッシュし、AI呼び出しは
**利用者が「更新する」ボタンを押したときのみ**（自動再生成はしない、コスト意識）。
更新検知のロジック（`_content_last_updated_at()`）は、意図的に `Project.updated_at` 自体を
比較対象から除外している。理由：相関図データもProjectテーブルの列のため、含めると
「保存した直後の自分自身の更新」を誤検知してしまうバグが実際に発生し、修正した経緯がある。

### 8. 尋問シナリオ生成（独立セッション方式によるカンニング防止）

`generate_interrogation()`（`app/ai_service.py`）。探偵役・容疑者役を**完全に別々のAI呼び出し**
として交互に実行し、以下を厳守する。

- 探偵役のプロンプトには `Project.constraints`（真相・犯人情報）を一切含めない
  （探偵が最初から答えを知っている状態を防ぐため）
- 容疑者役のプロンプトには、その容疑者専用の秘密（画面でその都度入力、**DBに保存しない**）と
  `Project.constraints` を渡す。他の容疑者の秘密は一切渡さない
- 1ターン＝探偵の発言＋容疑者の返答。指定ターン数（3〜20）繰り返し、
  `【名前】「セリフ」`形式で連結して1本の本文にする

この「情報を渡す相手を役割ごとに厳密に分ける」パターンは、今後similarな複数エージェント機能
（例：複数容疑者の同時尋問など）を作る場合にも踏襲すべき設計思想。

### 9. ミステリー・トリック生成モジュール（`docs/mystery_trick_module_design.md`）

事件のトリック・環境・小道具はPhase1〜3（＋配役ステップ）で生成し、本編の生成ルール
（`build_prompt()`の構造・書式規約）自体は変更しない。あくまで事件が絡む章にのみ
材料を追加供給する立場（詳細は設計書0.1節）。

## Gitの運用ルール

- `git commit` は、必ず利用者が明示的に依頼したときのみ実行すること。作業がひと区切りついても、
  確認なしで自動的にコミットしないこと。
- `git push` は絶対に自動実行しないこと。pushが必要な場合も、必ず事前に確認を取ること。
- コミットを依頼された場合は、変更内容を要約した分かりやすいコミットメッセージを付けること。
- 新しいブランチでの試行実装を依頼された場合、mainブランチを直接編集せず、
  必ず新しいブランチを切ってからそこで作業すること。

## 見送った・保留にした機能（安易に復活させない）

- **シナリオ分岐エンジン**（フラグ・選択肢・マルチエンディング・シーン管理）：
  今の「章が一直線に並ぶ」データモデルの根本的な拡張が必要な、別アプリ規模の話と説明済み。
  着手する場合は「まずフラグと選択肢だけ」のように小さく切り分けて相談すること。
- **尋問シナリオの選択肢化・証拠提示による分岐**：上記の分岐エンジンと同根の話として保留。
  現状は「1本道の尋問シナリオを生成する」機能のみ実装済み（詳細は上記「8.」参照）。
  完成した1本道の本文を土台に、後から分岐ポイントを見つけて代替案を追加生成する、という
  2段階アプローチが検討候補として出ているが、まだ設計・実装ともに未着手。
- **トリック提案AI・構造化難事件データベース（RAG）**：利用者の方針により不要と判断済み
  だったが、2026年7月の設計相談で方針転換し、**RAGなし版のトリック提案AI**として実装。
  過去事件のベクトル検索・類似トリック検索DBは引き続き作らない（AIの一般知識＋厳格な
  JSONスキーマ＋Phase5判定AIでの事後検証に任せる）。詳細設計は
  `docs/mystery_trick_module_design.md`を参照。
- **矛盾の自動検知（判定AI）**：尋問シナリオ生成と合わせて検討され、当初は見送り
  （独立セッション方式による「予防」を優先する方針だった）。2026年7月の設計相談で、
  ミステリー・トリック生成モジュールのPhase 5として実装することに方針転換。
  「独立セッション方式による予防」と「Phase5による事後検知」を**両立する二段構え**にする
  （予防を完璧にする必要はなく、漏れはPhase5で拾う前提でよい）。詳細は
  `docs/mystery_trick_module_design.md`の0.1節を参照。
- **画像生成**（背景・キャラクター立ち絵）：テキストシナリオ生成が落ち着くまでは後回し。
- **セリフの「？？？」正体隠し表記**：実装→利用者の指摘で撤回済み。ゲームエンジン側の役割。
- **キャラクターの「正体が明かされるまで？？？」判定**：AIの文脈判断のみに一旦頼る方針（(a)案）。
  章をまたいだ一貫性が崩れるようなら、`Character`に明示的な項目を追加する(b)案を検討する
  （まだ着手していない）。
- **ノードの手動配置の保存**（相関図）：保留中。実装する場合は`network.getPositions()`を
  キャラクター名に紐づけて保存する設計が既に検討済み。

## その他の運用上の注意

- 削除フォームにCSRFトークンを入れ忘れるバグが過去に複数箇所で発生した。
  新しいPOSTフォームを追加する際は、`{{ form.hidden_tag() }}` を使わない生のHTMLフォームの場合、
  `<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">` を忘れないこと。
- キャラクター名は同一プロジェクト内でユニークになるよう重複チェック済み
  （新規登録・改名・AI一括生成のいずれでも）。

## 詳細なユーザー向けドキュメント

機能ごとの使い方は `README.md` に記載されている。仕様変更時はこちらも合わせて更新すること。
